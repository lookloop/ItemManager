// 预留 — Grid/Container 短按拖拽无结算，最后一帧即结尾
// 如后续需要松手回弹动画等，在此扩展

namespace Lookloop.ItemManager
{
/// <summary>D 短按拖拽结算 — 预留（Grid/Container 最后一帧即结尾）</summary>
public static class D_ShortPressDragEnd
{
}
}
