using UnityEngine;
using UnityEngine.UI;
using TMPro;

namespace Lookloop.ItemManager
{
/// <summary>
/// Item 视觉 — 数据 → 画面同步。
/// 负责创建/更新/清空 Cell 内的 Item 显示（图标 + 光晕 + 数量文字）。
/// </summary>
public static class ItemSelfBuild
{
}
}
